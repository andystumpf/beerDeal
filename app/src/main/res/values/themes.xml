<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme (no androidx/material) so the app also builds where
         Google's Maven repo is unreachable. -->
    <style name="Theme.BeerDeal" parent="android:Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@color/amber_primary</item>
        <item name="android:colorAccent">@color/amber_primary</item>
    </style>
</resources>
